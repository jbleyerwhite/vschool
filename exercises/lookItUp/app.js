var dictionary = {};
dictionary.food = "eat me";
console.log(dictionary)


function addWord (word, definition){
  // use a line of code to add a property to the above object
  dictionary[word] = definition;
  // the property gets passed in as the funciton's word
  // and is equal to the function's definition
}

console.log(addWord("coffee", "black drink"))

    //create a function and store it in an object

        // use a line of code to add a property to the above object
  
        // the property gets passed in as the funciton's word
        // and is equal to the function's definition
        // the brackets around 'word' allow you to put info in it.  (vs using .word-your would need to define in the object)
      


  